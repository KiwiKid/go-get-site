package main

templ attributeSetResult(attributeResults []AttributeResult) {
	for _, ar := range attributeResults {
		<div id={ AttributeSetResultId(ar.WebsiteID, ar.PageID) } hx-swap-oob="true">
        OUT OF BAND UPDATE!
        </div>
	}
}

